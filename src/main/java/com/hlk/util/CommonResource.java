package com.hlk.util;

import java.io.IOException;
import java.util.Properties;

public class CommonResource {

  /*  //
    // load properties 文件
    //
    static Properties props = new Properties();

    static {
        try {
            props.load(CommonResource.class.getClassLoader().getResourceAsStream("application.properties"));
        } catch (IOException e) {
            e.printStackTrace();
            // 直接退出jvm
            System.exit(1);
        }
    }*/
    public static final String FILE_UPLOAD_PATH = "";


}
